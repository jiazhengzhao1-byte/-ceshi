# 威胁模型与权限边界

## 权限边界

- Service 以系统服务运行，拥有拦截连接的高权限。
- UI 以普通用户权限运行，仅通过 IPC 发出请求。

## 安全目标

- 不记录具体访问内容，仅记录状态切换/按钮点击/异常。
- IPC 仅暴露必要指令（解除/恢复/状态查询/配置重载）。
- 配置与状态目录仅允许管理员修改，UI 只读。

## 风险与缓解

- **服务崩溃导致失去拦截**：由 SCM 设置自动重启。
- **IPC 被滥用**：限制管道权限，仅允许本机当前用户组访问。
- **配置被篡改**：配置目录权限设置为管理员可写。

